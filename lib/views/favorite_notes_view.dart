import 'package:flutter/material.dart';

class FavoriteNotesView extends StatelessWidget {
  const FavoriteNotesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Favorite Notes'));
  }
}
